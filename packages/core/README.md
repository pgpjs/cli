# @pgpjs/core

Typed wrapper over **openpgp@6.3.1**, plus a directory keystore.

This package adds no cryptographic primitives. It returns values and throws `PgpjsError`. It does not print to stdout.
